package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Light Scheme Colors
val LightPrimary = Color(0xFF435E91) // Soft Royal Indigo blue
val LightSecondary = Color(0xFFD9E2FF) // Pale ice blue
val LightTertiary = Color(0xFF5B75A6) // Slate Blue
val LightBackground = Color(0xFFFDFBFF) // Pristine clean off-white
val LightSurface = Color(0xFFFFFFFF) // Pure White
val LightText = Color(0xFF1A1C1E) // Dark Charcoal Obsidian

// Dark Scheme Colors
val DarkPrimary = Color(0xFFADC6FF) // Crisp Light Steel Blue
val DarkSecondary = Color(0xFF2E4572) // Muted Deep Indigo
val DarkTertiary = Color(0xFF435E91) // Soft Blue Gray
val DarkBackground = Color(0xFF101318) // Absolute Dark Space
val DarkSurface = Color(0xFF1B1E24) // Minimal Pitch Card
val DarkText = Color(0xFFE2E2E6) // Bright platinum Gray

// Priority Colors
val PriorityHigh = Color(0xFFE55353) // Crimson
val PriorityMedium = Color(0xFFF09A37) // Golden Amber
val PriorityLow = Color(0xFF5AB664) // Oasis Green

// Category Colors (for visual decoration)
val CatWork = Color(0xFF3B82F6) // Work (Blue)
val CatPersonal = Color(0xFFEC4899) // Personal (Pink)
val CatHealth = Color(0xFF10B981) // Health (Emerald)
val CatStudy = Color(0xFF8B5CF6) // Study (Purple)
